export class Ingredient {
    
    //public name: string;
    //public amount: number;

    /**
     * Defined constructor with PUBLIC type to replace attribute declaration
     * @param name 
     * @param amount 
     */
    constructor(public name: string, public amount: number){   }
}